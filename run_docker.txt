# Docker 開啟 OR 關閉

# 1. 抓取最新程式碼
git pull

# 2. 使用 Compose 自動建置並啟動 (加上 --build 會強制重新檢查 Dockerfile 變動)
# -d 代表背景執行
docker-compose up -d --build

# 3. 查看執行紀錄
docker-compose logs -f

# 查看執行紀錄，按下 ESC 退出
docker logs -f genshin_god_field